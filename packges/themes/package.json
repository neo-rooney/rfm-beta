{
  "name": "@rfm-ui/themes",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "concurrently \"npm:dev:type\" \"npm:dev:js\"",
    "dev:js": "node build.js --dev --watch",
    "dev:type": "tsc --emitDeclarationOnly --watch",
    "build": "npm run build:js && npm run build:type",
    "build:js": "node build.js",
    "build:type": "tsc --emitDeclarationOnly"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  },
  "author": "rooney",
  "license": "ISC"
}
